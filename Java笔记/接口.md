1、匿名内部类在**创建对象**的时候，只能使用唯一一次

2、匿名对象，在**调用方法**的时候只能调用唯一一次

``` java
new myInterface(){
    @Override
    public void method1(){
        ;
    }
}.method1();
//既是用了匿名对象又使用了匿名内部类
```

3、匿名内部类是省略了**实现类/子类名称**，但是匿名对象是省略了**对象名称**

**匿名内部类与匿名对象不是一回事！**